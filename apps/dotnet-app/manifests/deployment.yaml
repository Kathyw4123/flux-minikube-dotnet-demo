apiVersion: apps/v1
kind: Deployment
spec:
  template:
    spec:
      containers:
      - name: dotnet-app
        image: dotnet-app:latest  # Local image name
        imagePullPolicy: Never    # Critical - prevents Kubernetes from looking online